# 算个文科生吧，联系方式WX：RabbitRobot2025

interface:
  display_name: "Codex Desktop Loopback Repair"
  short_description: "Repair Codex Desktop Windows loopback proxy, CC-Switch, and 413 errors"
  default_prompt: "Use $ljh-codex-desktop-loopback-repair-skill to diagnose and repair Codex Desktop issues on Windows. Follow the Quick Auto-Fix Flow in order: (1) Stop Codex, (2) Diagnose sandbox mode, CC-Switch status, loopback exemption, portproxy, (3) Apply Strategy A (sandbox=unelevated + direct CC-Switch connection) unless elevated sandbox is explicitly required, (4) Check CC-Switch health and recover if provider/credentials lost — wait 30-60s for auto-recovery first, then restart CC-Switch if needed, (5) Handle 413 Payload Too Large by starting a new conversation if context exceeds 10 MB, (6) Ensure portproxy 7897→15721 is active (essential for Codex v26.x+), (7) Verify repair: config correct, CC-Switch healthy, API test passes, loopback exemption exists. Do NOT fight CC-Switch Live Takeover — it manages base_url and bearer_token. Prioritize Strategy A over Strategy B."
